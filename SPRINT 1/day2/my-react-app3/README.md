# Reusable Card Component – React Assignment

This project demonstrates how to build a **reusable Card component** in React that accepts a `title` and dynamic content using the `children` prop. It showcases component reusability and clean UI structuring using simple JSX composition.

---

## 🚀 Features

- Reusable **Card** component
- Accepts `title` (string) as a prop
- Accepts `children` to display dynamic content inside the card
- Three different Card components rendered in the parent (App.jsx)
- Clean and simple UI structure

---

## 📁 Project Structure

